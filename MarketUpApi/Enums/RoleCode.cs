using System.ComponentModel.DataAnnotations;

namespace MarketUpApi.Enums
{
    public enum RoleCode
    {
        //[Display(Name = "MK", ResourceType = typeof(MainResource))]
        ST,
        DT,
        MK,      
        CS,
    }
}
